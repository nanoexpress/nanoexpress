const passportInitialize = require('./passport');
const reDoc = require('./redoc');
const bodyParser = require('./body-parser');
const swaggerUi = require('./swagger-ui');

module.exports = { passportInitialize, reDoc, bodyParser, swaggerUi };
